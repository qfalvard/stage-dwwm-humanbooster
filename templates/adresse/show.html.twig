{% extends 'base.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block body %}
<div class="encadre">
    <h1>Adresse</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Ligne1</th>
                <th>Adresse</th>
                <th>Ville</th>
                <th>Code Postal</th>
                <th>Commune</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ adresse.ligne1 }}</td>
                <td>{{ adresse.ligne2 }}</td>
                <td>{{ adresse.ligne3 }}</td>
                <td>{{ adresse.cp }}</td>
                <td>{{ adresse.commune.nom|upper }}</td>
                 <td>
                    <a href="{{ path('adresse_edit', {'id': adresse.id}) }}"><i class="far fa-edit"></i></a>
                </td>
            </tr>
        </tbody>
    </table>
    <div class="container container-fluid row">
        <div class="col-md-3 mt-2">
            <a href="{{ path('adresse_index') }}">Retour à la liste d'adresse</a>
        </div>
        <div class="col-md-3 ml-auto">
            {{ include('adresse/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
