{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row px-3">
        <div class="mt-3">
            <a class="p-2 secondaire" href="{{ path('recherche_stage_index', {'session_id': session_id}) }}">Retour à la liste recherche de stage</a>
        </div>
        <div class="ml-auto">
            <button class="primaire">{{ button_label|default('Valider') }}</button>
        </div>
{{ form_end(form) }}