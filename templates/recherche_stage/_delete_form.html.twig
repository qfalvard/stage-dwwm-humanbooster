<form method="post" action="{{ path('recherche_stage_delete', {'id': recherche_stage.id , 'session_id':session_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recherche_stage.id) }}">
    <button class="btnSupprimer">Supprimer</button>
</form>
