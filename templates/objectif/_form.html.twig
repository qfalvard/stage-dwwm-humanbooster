{{ form_start(form) }}
{{ form_widget(form) }}
<div class="row px-3">
    <div class="mt-3">
        <a class="secondaire p-2" href="{{ path('titre_professionnel_show', {'id': titre.id}) }}">Retour au titre professionnel</a>
    </div>
    <div class="ml-auto">
        <button class="primaire">Valider</button>
    </div>
</div>
{{ form_end(form) }}