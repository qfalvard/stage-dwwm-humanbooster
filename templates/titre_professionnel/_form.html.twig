{{ form_start(form) }}
{{ form_widget(form) }}
<div class="row px-3">
    <div class="mt-3">
        <a class="p-2 secondaire" href="{{ path('titre_professionnel_index') }}">Retour à la liste des titres Professionnels</a>
    </div>
    <div class="ml-auto">
        <button class="primaire">{{ button_label|default('Valider') }}</button>
    </div>
</div>
{{ form_end(form) }}