{% extends 'base.html.twig' %}

{% block title %}Modifier CCP{% endblock %}

{% block body %}
<div class="encadre">
    <h1>Modifier le CCP</h1>
    <div class="row px-3">
        <div class="ml-auto">
            {% if app.user.roles[0] == 'ROLE_ENCADRANT' %}
                {{ include('ccp/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>
    {{ include('ccp/_form.html.twig', {'button_label': 'Enregistrer'}) }}    
</div>
{% endblock %}