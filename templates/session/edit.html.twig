{% extends 'base.html.twig' %}

{% block title %}Editer une session{% endblock %}

{% block body %}
<div class="encadre mb-5">
    <h1>Editer une session</h1>
    <div class="row px-3">
        <div class="ml-auto">
            {{ include('session/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('session/_form.html.twig', {'button_label': 'Valider'}) }}
    
</div>
{% endblock %}