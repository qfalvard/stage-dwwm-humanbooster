<form method="post" action="{{ path('session_delete', {'id': session.id}) }}"
    onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette session ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.id) }}">
    <a type="submit"><button class="btnSupprimer">Supprimer</button></a>
</form>