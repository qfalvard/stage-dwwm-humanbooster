{{ form_start(form) }}
{{ form_row(form.intitule, { 'label': 'Intitulé' }) }}
{{ form_row(form.dateDebut, { 'label': 'Date de début' }) }}
{{ form_row(form.dateFin, { 'label': 'Date de fin' }) }}
{{ form_row(form.debutStage, { 'label': 'Date de début de stage' }) }}
{{ form_row(form.finStage, { 'label': 'Date de fin stage' }) }}
{{ form_row(form.Moodle, { 'label': 'Lien Moodle' }) }}
{{ form_row(form.adresse) }}

{{ form_widget(form) }}
<div class="row px-3">
    <div class="mt-3">
        <a class="p-2 secondaire" href="{{ path('session_index') }}">Retour à la liste des sessions</a>
    </div>
    <div class="ml-auto">
        <button class="primaire">{{ button_label|default('Valider') }}</button>
    </div>
</div>
{{ form_end(form) }}