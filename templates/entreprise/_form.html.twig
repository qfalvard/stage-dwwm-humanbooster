{{ form_start(form) }}
{{ form_widget(form) }}
<div class="row px-3">
	<div class="mt-3">
		{% if session_id is defined %}
			<a class="p-2 secondaire" href="{{ path('recherche_stage_index', {'session_id':session_id}) }}">Retour à la liste des recherches de stage</a>
		{% else %}
			<a class="p-2 secondaire" href="{{ path('recherche_stage_index')}}">Retour à la liste des recherches de stage</a>
		{% endif %}
	</div>
	<div class="ml-auto">
		<button class="primaire">{{ button_label|default('Valider') }}</button>
	</div>
	{{ form_end(form) }}
